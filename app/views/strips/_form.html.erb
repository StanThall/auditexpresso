<%= form_for(strip) do |f| %>
  <% if strip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(strip.errors.count, "error") %> prohibited this strip from being saved:</h2>

      <ul>
      <% strip.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title, style: "width:100%;max-width:100%;" %>
  </div>

  <div class="field">
    <%= f.label :catch %>
    <%= f.text_field :catch, style: "width:100%;max-width:100%;" %>
  </div>

  <div class="field">
    <%= f.label :content %>
    <%= f.text_area :content, style: "width:100%;max-width:100%;height:300px;" %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
